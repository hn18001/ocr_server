1. 如果在10.1.243.161(ubuntu)上启动服务，10.123.16.51（centos）上无法连接到；反之可以。
2. TProcessPoolServer模式中，初始状态下，启动的进程不会初始化，每个进程都需要call一次以后才会启动，第一次由于要加载Lua中的cudnn等模块，速度会很慢，后续就不会加载这些模块，速度就快很多。
3. 增加异常处理；
4. 如果线程池中有一个线程发生异常，如何获取线程的状态，并重新启动一个线程？
